
kubectl get ns auristorfs-client

if [ $? -ne 0 ]; then 
        echo NAMESPACE: auristorfs-client
        echo Exiting, namespace not found
        exit 1; 
fi

helm install  -n auristorfs-client auristorfs-csi-driver .
